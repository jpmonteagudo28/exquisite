% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stretch.R
\name{stretch}
\alias{stretch}
\title{Update the Long URL of an Existing TinyURL Link}
\usage{
stretch(long_url, domain = "tinyurl.com", alias = NULL)
}
\arguments{
\item{long_url}{A character string representing the new long URL to associate with the given alias.}

\item{domain}{A character string specifying the domain of the shortened URL. Default is \code{"tinyurl.com"}. You can also specify \code{"tiny.one"}.}

\item{alias}{A character string specifying the alias (existing shortened URL) to update. This alias must already exist and will be updated to point to the new \code{long_url}.}
}
\value{
A list containing the updated information, including the updated shortened URL.
}
\description{
This premium function allows you to \strong{update the long URL} associated with an existing shortened URL (alias) on TinyURL. It requires a valid API token, which is retrieved from the \code{.Renviron} file. This function is \strong{not available to free users}; a paid TinyURL account is required to use this feature.
}
\details{
This function sends a \code{PATCH} request to TinyURL’s \verb{/change} API endpoint, which updates the long URL for an existing short link. The new long URL is associated with the existing alias. \strong{This is a premium feature}, meaning free users \strong{cannot access this function}.

\strong{Features:}
\itemize{
\item Updates the long URL for an existing TinyURL.
\item Requires a valid API token, stored in \code{.Renviron}.
\item A premium TinyURL account is needed to use this function.
\item Returns the updated shortened URL in the response.
}

\strong{Limitations:}
\itemize{
\item Free users cannot access this function. A premium TinyURL account is required.
\item The alias (\code{alias}) must exist in the TinyURL system.
}
}
\note{
Before using this function, ensure that the API token is stored in your \code{.Renviron} file using the \code{manage_tinyurl_token()} function.
}
\examples{
\dontrun{
# Update the long URL for an existing TinyURL alias (premium feature)
updated_url <- stretch("https://newlongurl.com", domain = "tinyurl.com", alias = "existingalias")
print(updated_url)  # This will print the updated shortened URL
}

}
\seealso{
\code{\link{manage_tinyurl_token}} for setting the TinyURL API token.
}
